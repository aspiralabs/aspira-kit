packages:
  - packages/*
  - apps/*

# pnpm 10+ refuses to run dependency build scripts unless they are listed. Neither
# of these needs its postinstall for our use, so they are ignored explicitly.
ignoredBuiltDependencies:
  - core-js-pure
  - unrs-resolver
